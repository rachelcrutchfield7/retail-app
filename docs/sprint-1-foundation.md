# Sprint 1 Foundation Notes

Sprint 1 establishes authentication, navigation, theme tokens, base components, and service boundaries.

Install these dependencies when network access is available:

```text
expo-router
@supabase/supabase-js
zustand
@tanstack/react-query
react-hook-form
zod
lucide-react-native
```

The local preview keeps lightweight in-repo fallbacks for Supabase, React Query, and auth state so the app remains runnable before the external services are connected.
